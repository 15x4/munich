[build]
  command = "node -v && yarn generate 2>&1 && echo '--- listing .output ---' && ls -la .output/ && ls -la .output/public/ | head -20"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "22"
